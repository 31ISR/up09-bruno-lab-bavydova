info:
  name: Items
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/items"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
